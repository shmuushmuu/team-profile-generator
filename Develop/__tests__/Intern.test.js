const Intern = require("../lib/Intern");

describe("Interns are employees who are still in school", () => {
    const intern = new Intern('Sam,' 0155, 'shmuushmuu@gmail.com', '1203456789');

    expect(engineer.gitHub).toEqual(expect.any(String));
});
